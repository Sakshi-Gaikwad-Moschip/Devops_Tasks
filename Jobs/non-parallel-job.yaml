apiVersion: batch/v1
kind: Job
metadata:
  name: non-parallel-job
spec:
  template:
    spec:
      containers:
      - name: hello
        image: busybox
        command: ["echo", "Hello from a non-parallel job"]
      restartPolicy: Never
  backoffLimit: 4
